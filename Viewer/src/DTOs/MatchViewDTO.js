

export default class MatchViewDTO{
    constructor(id, scoreMap, concluded) {
        this.id = id;
        this.scores = scoreMap;
        this.concluded = concluded;
    }

//    val scores: Map<CompetitorViewDto, Int>,

}